{
  "name": "searchsphere-backend",
  "version": "1.0.0",
  "description": "SearchSphere search engine API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node src/db/migrate.js",
    "db:clear": "node src/db/clear.js",
    "db:size": "node -e \"require('dotenv').config(); require('./src/db/storage').getStorageStats().then(s=>{console.log(require('./src/db/storage').formatStorageLine(s)); require('./src/config/database').pool.end()})\"",
    "seed": "node src/db/seed.js",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cheerio": "^1.0.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^8.0.11",
    "pg": "^8.13.0",
    "robots-parser": "^3.0.1",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
